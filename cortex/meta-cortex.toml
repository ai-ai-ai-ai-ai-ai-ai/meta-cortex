# Execution settings for Meta-Cortex agents.
# Settings apply by role, regardless of agent directory.

[gizmo-prime]
model = "gpt-6-luna"
reasoning_effort = "max"

[team.gizmo]
model = "gpt-6-luna"
reasoning_effort = "max"

[team.agent]
model = "gpt-6-luna"
reasoning_effort = "max"
